name: Build and Push Docker Image

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version/Tag (leer lassen für auto-increment)'
        required: false
        default: ''

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    outputs:
      version: ${{ steps.bump_version.outputs.version }}
      is_os_patch: ${{ steps.bump_version.outputs.is_os_patch }}

    strategy:
      matrix:
        include:
          - dockerfile: ./Dockerfile
            suffix: ""
          - dockerfile: ./Dockerfile.alpine
            suffix: "-alpine"

    steps:
      - name: Datum ermitteln
        id: date
        run: echo "date=$(date -u +"%Y-%m-%dT%H:%M:%SZ")" >> $GITHUB_OUTPUT

      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Version berechnen (Auto-Increment Patch)
        id: bump_version
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v1.0.0")
          echo "Letzter bekannter Tag: $LATEST_TAG"

          if [ "${{ github.event_name }}" = "push" ] && [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TARGET_VERSION="${{ github.ref_name }}"
          elif [ -n "${{ inputs.version }}" ]; then
            TARGET_VERSION="${{ inputs.version }}"
          else
            CLEAN_TAG="${LATEST_TAG#v}"
            IFS='.' read -r MAJOR MINOR PATCH <<< "$CLEAN_TAG"
            PATCH=$(echo "$PATCH" | grep -o '^[0-9]*')
            NEW_PATCH=$((PATCH + 1))
            TARGET_VERSION="v${MAJOR}.${MINOR}.${NEW_PATCH}"
          fi

          echo "Bauen mit Ziel-Version: $TARGET_VERSION"
          echo "version=$TARGET_VERSION" >> $GITHUB_OUTPUT

          # Eine vierte Versionsstelle (vX.Y.Z.N) ist ausschliesslich fuer
          # os-patch-release.yml's monatliche, rein OS-Patch-getriebene
          # Releases reserviert (siehe dortiger Kommentar) - da sich dabei
          # nur das Docker-Basis-Image aendert, nicht dieses Package, waere
          # ein erneuter .deb-Build reines Rauschen (identischer Inhalt,
          # neue Versionsnummer).
          DOT_COUNT=$(grep -o "\." <<< "$TARGET_VERSION" | wc -l)
          if [ "$DOT_COUNT" -ge 3 ]; then
            echo "is_os_patch=true" >> $GITHUB_OUTPUT
          else
            echo "is_os_patch=false" >> $GITHUB_OUTPUT
          fi

      - name: Log in zur GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker Metadaten extrahieren
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}},value=${{ steps.bump_version.outputs.version }}
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Tag-Namen definieren
        id: vars
        run: |
          echo "IMAGE_TAG=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.bump_version.outputs.version }}${{ matrix.suffix }}" >> $GITHUB_ENV
          echo "LATEST_TAG=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest${{ matrix.suffix }}" >> $GITHUB_ENV

      - name: Build und Push Docker Image (Multi-Arch)
        uses: docker/build-push-action@v7.4.0
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          pull: true
          tags: ${{ env.IMAGE_TAG }},${{ env.LATEST_TAG }}
          provenance: false
          build-args: |
            VERSION=${{ steps.bump_version.outputs.version }}
            BUILD_DATE=${{ steps.date.outputs.date }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: GitHub Release & Git-Tag erstellen
        if: matrix.suffix == '' # Verhindert, dass das Release doppelt erstellt wird
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.bump_version.outputs.version }}
          name: Release ${{ steps.bump_version.outputs.version }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-deb:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: needs.build-and-push.outputs.is_os_patch != 'true'
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: fpm installieren
        run: sudo gem install --no-document fpm

      - name: Package per pip in ein Zielverzeichnis installieren
        run: python3 -m pip install --no-deps --no-cache-dir --target=install .

      - name: .deb-Verzeichnisstrukturen aufbauen
        run: |
          VERSION="${{ needs.build-and-push.outputs.version }}"
          echo "DEB_VERSION=${VERSION#v}" >> "$GITHUB_ENV"

          # Shebang auf den System-Python fixen - pip schreibt sonst den
          # Interpreter-Pfad DIESES Runners rein, der auf einer echten
          # Debian/Ubuntu-Installation nicht existiert.
          sed -i '1s|.*|#!/usr/bin/python3|' install/bin/yt-upload install/bin/get-token
          rm -rf install/yt_upload/__pycache__ install/get_token/__pycache__

          # Zwei getrennte .deb-Pakete statt einem: "yt-upload" enthaelt CLI,
          # Python-Package und Config (alles, was auch der rein manuelle
          # Datei-Modus braucht), "yt-upload-daemon" nur den systemd-Service
          # samt Systemuser-Anlage. So zieht sich ein reiner CLI-User (der nur
          # `yt-upload video.mp4` aufruft) nicht automatisch python3-inotify,
          # den dedizierten Systemuser und den nie aktivierten systemd-Service
          # mit rein.

          # /usr/lib/python3/dist-packages statt eines eigenen PYTHONPATH-
          # Verzeichnisses (wie im Dockerfile) - das ist bei einem .deb der
          # Standard-Suchpfad von Debians System-Python, beide Packages sind
          # damit ohne zusätzliche Umgebungsvariablen importierbar. get_token
          # läuft hier (anders als im Dockerfile) als regulärer, per pip
          # installierter Entry-Point statt als bare-copy-Skript - für ein
          # .deb, das ohnehin das ganze Package installiert, unnötig, das
          # nachzubilden.
          mkdir -p pkgroot-cli/usr/bin pkgroot-cli/usr/lib/python3/dist-packages pkgroot-cli/etc/yt-upload/conf.d
          cp install/bin/yt-upload install/bin/get-token pkgroot-cli/usr/bin/
          chmod 755 pkgroot-cli/usr/bin/yt-upload pkgroot-cli/usr/bin/get-token
          cp -r install/yt_upload pkgroot-cli/usr/lib/python3/dist-packages/yt_upload
          cp -r install/get_token pkgroot-cli/usr/lib/python3/dist-packages/get_token
          cp -r install/yt_upload-*.dist-info pkgroot-cli/usr/lib/python3/dist-packages/
          cp upload.conf.example pkgroot-cli/etc/yt-upload/upload.conf

          mkdir -p pkgroot-daemon/usr/lib/systemd/system
          cp yt-upload.service pkgroot-daemon/usr/lib/systemd/system/yt-upload.service

      - name: yt-upload (CLI) .deb bauen
        run: |
          fpm -s dir -t deb -f \
            -n yt-upload \
            -v "$DEB_VERSION" \
            -a all \
            --license GPL-3.0 \
            --maintainer "chaos7x" \
            --description "Automatisierter YouTube Upload Workflow (CLI)" \
            --url "https://github.com/${{ github.repository }}" \
            --depends python3 \
            --depends python3-requests \
            --depends ffmpeg \
            --config-files /etc/yt-upload/upload.conf \
            -C pkgroot-cli \
            usr etc

      - name: yt-upload-daemon .deb bauen
        run: |
          fpm -s dir -t deb -f \
            -n yt-upload-daemon \
            -v "$DEB_VERSION" \
            -a all \
            --license GPL-3.0 \
            --maintainer "chaos7x" \
            --description "Systemd-Dienst fuer den yt-upload-Daemon-Modus (yt-upload -D)" \
            --url "https://github.com/${{ github.repository }}" \
            --depends "yt-upload = $DEB_VERSION" \
            --depends python3-inotify \
            --depends adduser \
            --after-install daemon-postinst.sh \
            --before-remove daemon-prerm.sh \
            --after-remove daemon-postrm.sh \
            -C pkgroot-daemon \
            usr

      - name: .deb-Pakete als Release-Assets hochladen
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.build-and-push.outputs.version }}
          files: "*.deb"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
